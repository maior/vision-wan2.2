# 📊 품질 평가 메트릭 상세 설명

## 🎓 학계 표준 메트릭

### 1. CLIP Score (목표: ≥ 0.3)

**정의:**
- Text-Video Alignment (텍스트-비디오 의미적 일치도)
- OpenAI의 CLIP 모델을 사용하여 캡션과 비디오 프레임 간 유사도 측정

**계산 방법:**
```python
import clip
import torch

model, preprocess = clip.load("ViT-B/32")
frames = [preprocess(f) for f in video_frames]
text = clip.tokenize([caption])

with torch.no_grad():
    frame_features = model.encode_image(frames)
    text_features = model.encode_text(text)

similarity = (frame_features @ text_features.T).mean()
clip_score = similarity.item()
```

**범위:** 0~1 (높을수록 좋음)

**해석:**
- **0.30+**: 우수 (목표 달성) ✅
- **0.25-0.29**: 양호 (개선 권장)
- **0.20-0.24**: 보통 (재작업 필요)
- **<0.20**: 불량 (사용 불가) ❌

**의미:**
- 캡션이 비디오 내용을 얼마나 정확하게 묘사하는지 정량화
- 모델 학습 시 텍스트 조건이 잘 학습될 수 있는지 판단
- CLIP Score가 낮으면 생성 모델이 텍스트 프롬프트를 제대로 따르지 못함

**실행 방법:**
```bash
# 1. 설치
pip install clip-openai torch torchvision

# 2. 스크립트 실행
python scripts/calculate_clip_score.py \
  --data_path ./preprocessed_data/all_train.csv \
  --sample_size 1000 \
  --model_name ViT-B/32 \
  --output clip_scores.json

# 3. 결과 확인
cat clip_scores.json
```

---

### 2. FVD (Fréchet Video Distance, 목표: ≤ 1140)

**정의:**
- Video Quality Metric (비디오 품질 지표)
- 생성된/데이터셋 비디오와 실제 비디오 간의 통계적 거리 측정
- FID (Fréchet Inception Distance)의 비디오 버전

**계산 방법:**
```
FVD = ||μ_real - μ_gen||² + Tr(Σ_real + Σ_gen - 2√(Σ_real·Σ_gen))

여기서:
- μ_real: 실제 비디오의 I3D 특징 평균
- μ_gen: 데이터셋 비디오의 I3D 특징 평균
- Σ_real: 실제 비디오의 공분산 행렬
- Σ_gen: 데이터셋 비디오의 공분산 행렬
- Tr: 대각합 (Trace)
```

**범위:** 0~∞ (낮을수록 좋음)

**해석:**
- **<1140**: 우수 (목표 달성) ✅
- **1140-1500**: 양호 (개선 권장)
- **1500-2000**: 보통 (재작업 필요)
- **>2000**: 불량 (사용 불가) ❌

**의미:**
- 비디오의 시각적 품질과 다양성을 평가
- 낮은 FVD = 실제 비디오와 유사한 분포 = 고품질 데이터셋
- 높은 FVD = 비정상적이거나 저품질 비디오 다수 포함

**실행 방법:**
```bash
# 1. 설치
pip install tensorflow tensorflow-gan

# 2. I3D 특징 추출
python scripts/extract_i3d_features.py \
  --real_videos /path/to/reference_videos \
  --dataset_videos /path/to/your_videos \
  --output features.npz

# 3. FVD 계산
python scripts/calculate_fvd.py \
  --features features.npz \
  --output fvd_score.json

# 4. 결과 확인
cat fvd_score.json
```

---

## 🎯 현재 데이터셋 상태

### 측정 필요 항목
- ⚠️ **CLIP Score**: 미측정 → 샘플링하여 계산 필요
- ⚠️ **FVD**: 미측정 → Reference 비디오셋 준비 후 계산

### 일괄 평가 스크립트
```bash
# 모든 메트릭 한 번에 계산
python scripts/evaluate_dataset_quality.py \
  --data_csv ./preprocessed_data/all_train.csv \
  --sample_size 5000 \
  --metrics clip,fvd \
  --output quality_report.json

# 예상 소요 시간: 2-3시간 (V100 GPU 필요)
```

---

## 📚 기타 품질 메트릭

### 우리가 현재 사용 중인 메트릭 (데이터셋 수준)

**1. 캡션 품질 (가중치 35%)**
- 길이 적절성: 20-300자 권장
- 어휘 다양성: TTR (Type-Token Ratio)
- 구체성: 구체적 명사/동작 묘사
- 문법 정확성: 문장 완결성

**2. 메타데이터 완전성 (가중치 25%)**
- 필수 필드 완성도: clip_id, media_type, file_path, caption, resolution, length
- 선택 필드 완성도: category, keyword

**3. 파일 무결성 (가중치 25%)**
- 파일 존재 여부
- 파일 크기 유효성
- 파일 읽기 가능 여부

**4. 데이터 균형성 (가중치 15%)**
- 카테고리 분포 Entropy
- 해상도 분포 균형
- 비디오 길이 분산

**현재 종합 점수: 91.8%** (A등급)

---

## 🚀 품질 향상 로드맵

### Phase 1: 학계 표준 메트릭 측정 (1주)
1. CLIP Score 계산 스크립트 작성 및 실행
2. Reference 비디오셋 준비 (예: Kinetics-700, WebVid)
3. FVD 계산 파이프라인 구축
4. 결과 대시보드에 통합

**목표:**
- CLIP Score ≥ 0.3
- FVD ≤ 1140

### Phase 2: 캡션 품질 고도화 (2주)
1. KoNLPy/Mecab 형태소 분석 통합
2. LLM 기반 캡션 품질 자동 평가 (GPT-4, Claude)
3. 저품질 캡션 재작성 파이프라인
4. 캡션 가이드라인 문서화

**예상 효과:** CLIP Score +0.05~0.08

### Phase 3: 파일 무결성 자동화 (1주)
1. ffprobe 기반 비디오 메타데이터 추출
2. 손상 파일 자동 탐지
3. 파일 해시 DB 구축
4. 주기적 검증 크론잡

**예상 효과:** FVD -50~100

### Phase 4: 데이터 증강 및 균형 조정 (2주)
1. 과소표 카테고리 증강
2. 해상도 정규화 (1280×720, 1920×1080 → Wan2.2 지원 해상도)
3. Train/Val/Test 분할 재조정

**예상 효과:** 전체 품질 +2~3%

---

## 📖 참고 문헌

1. **CLIP: Learning Transferable Visual Models From Natural Language Supervision**
   - Radford et al., OpenAI, 2021
   - https://arxiv.org/abs/2103.00020

2. **Towards Accurate Generative Models of Video: A New Metric & Challenges**
   - Unterthiner et al., DeepMind, 2018
   - FVD 메트릭 소개
   - https://arxiv.org/abs/1812.01717

3. **GANs Trained by a Two Time-Scale Update Rule Converge to a Local Nash Equilibrium**
   - Heusel et al., 2017
   - FID (FVD의 기반) 메트릭
   - https://arxiv.org/abs/1706.08500

4. **I3D: Quo Vadis, Action Recognition? A New Model and the Kinetics Dataset**
   - Carreira & Zisserman, Google DeepMind, 2017
   - FVD 계산에 사용되는 I3D 특징 추출기
   - https://arxiv.org/abs/1705.07750

---

## 💡 실무 팁

### CLIP Score 최적화
- **캡션 구체화**: "사람이 걷는다" → "한 남성이 공원 산책로를 여유롭게 걷고 있다"
- **시간적 묘사**: "차가 달린다" → "빨간 스포츠카가 고속도로를 빠르게 질주한다"
- **배경 포함**: "고양이가 논다" → "회색 고양이가 거실 소파에서 털실을 가지고 논다"

### FVD 최적화
- **해상도 일관성**: 모든 비디오를 동일 해상도로 정규화
- **프레임레이트 통일**: 30fps 또는 60fps로 통일
- **손상 파일 제거**: 깨진 프레임, 과도한 압축, 아티팩트 제거
- **카메라 움직임 균형**: 정적/동적 비디오 적절히 혼합

### Reference 비디오셋 선택
- **Kinetics-700**: 액션 인식 데이터셋, 다양한 동작
- **WebVid-10M**: 텍스트-비디오 쌍, 일상적 장면
- **UCF-101**: 액션 클립, 짧은 동작 중심
- **내부 고품질 셋**: 직접 촬영한 고해상도 비디오 (추천)

---

## ✅ 체크리스트

### 데이터셋 품질 확인
- [ ] CLIP Score ≥ 0.3
- [ ] FVD ≤ 1140
- [ ] 캡션 품질 ≥ 90%
- [ ] 메타데이터 완성도 ≥ 95%
- [ ] 파일 무결성 ≥ 99%
- [ ] 카테고리 균형 (Entropy > 3.0)

### 스크립트 준비
- [ ] `scripts/calculate_clip_score.py`
- [ ] `scripts/extract_i3d_features.py`
- [ ] `scripts/calculate_fvd.py`
- [ ] `scripts/evaluate_dataset_quality.py`

### 인프라
- [ ] GPU 환경 (V100 × 2 이상)
- [ ] Reference 비디오셋 준비
- [ ] 충분한 디스크 공간 (특징 추출용)

---

**대시보드에서 확인:** http://211.180.253.250:7020/quality/methodology
